
version: '3'
services:
  node:
    image: ethereum/client-go:latest
    command: --datadir /data --networkid 1 --http --http.addr 0.0.0.0 --http.port 8546 --http.corsdomain "*" --http.api eth,web3,personal,net --allow-insecure-unlock --nodiscover
    volumes:
      - ./data:/data
    ports:
      - "8546:8546"
      - "30303:30303"
networks:
  default:
    name: eth-net-1
